<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.backend.wallet.model.WalletMapper">

    <!-- 거래 내역 조회 -->
    <select id="selectWalletHistory">
        SELECT
            wh.id,
            transaction_type,
            goods_id,
            description,
            amount,
            wh.created_at
        FROM wallet_history wh
            INNER JOIN coin_wallet cw
                ON cw.id = wh.wallet_id
        WHERE cw.user_id = #{loginUserId}
        ORDER BY wh.created_at desc
        LIMIT #{size} OFFSET #{offset};
    </select>

    <!-- 거래 내역 총 개수 조회 -->
    <select id="countWalletHistory">
        SELECT
            count(*)
        FROM wallet_history wh
            INNER JOIN coin_wallet cw
                ON cw.id = wh.wallet_id
        WHERE cw.user_id = #{loginUserId}
    </select>

    <!-- 자산 현황 조회 -->
    <select id="selectCoinWallet">
        SELECT
            balance,
            locked_balance
        FROM coin_wallet
        WHERE user_id = #{loginUserId}
    </select>

    <!-- 지갑 생성 -->
    <insert id="createCoinWallet" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO coin_wallet (
        user_id,
        balance,
        locked_balance
    )
    VALUES (
        #{loginUserId},
        0,
        0
    )
    </insert>

    <!-- 코인 충전(임시) -->
    <update id="chargeCoin">
        UPDATE coin_wallet
        SET balance = balance + #{coinAmount}
        WHERE user_id = #{loginUserId}
    </update>
</mapper>